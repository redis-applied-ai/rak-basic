OPENAI_API_KEY=your-key-here

# redis-agent-kit >= 0.1.1 enables memory by default and requires agent-memory-server.
# That package is only resolvable on Python 3.12; on 3.11 leave memory off.
RAK_MEMORY__ENABLED=false
